# Worker Environment Configuration
# Copy this file to .env.worker on the remote worker machine

# Main Server IP Address (where MongoDB, NSQ, and main backend are running)
MAIN_SERVER_IP=172.12.0.132

# MongoDB Credentials (must match the main server)
# Note: Password is URL-encoded (@ becomes %40)
MONGO_USERNAME=gvpocr_admin
MONGO_PASSWORD=gvp%40123

# Worker Identification (optional, will auto-generate if not set)
WORKER_ID=

# LLM Model Configuration (optional - set to true to enable local LLM services)
OLLAMA_ENABLED=false
LLAMACPP_ENABLED=false

# Model selections for local LLMs
OLLAMA_MODEL=llama3.2-vision
LLAMACPP_MODEL=gemma3-vision

# HuggingFace token for LlamaCPP model downloads
HUGGING_FACE_HUB_TOKEN=

# Optional: Override default provider hosts if needed
# OLLAMA_HOST=http://ollama:11434
# LLAMACPP_HOST=http://llamacpp:8000
# VLLM_HOST=http://172.12.0.132:8000
